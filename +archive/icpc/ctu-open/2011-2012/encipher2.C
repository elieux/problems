#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

#define LINE_LENGTH 10001

void doit(int c, char line[LINE_LENGTH]) {
	vector<char> result;
	for (int i = 0; i < len(line); i++)
	{	
	//	if (line[i] < 
	//	result.push
	}
}

int main(int argc, char *argv[]) {
	char line[LINE_LENGTH];
	int ciph ;
	
	argc = argc;
	argv = argv;
	
	while (true)
	{
		scanf("%d\n", ciph);
		if (ciph == 0)
			break;
		fgets(line, LINE_LENGTH, stdin);
		//~ sscanf(line, "");
		doit(ciph, line);
		printf("%d %d\n", 'a', 'A');
		//~ if ()
			//~ break;
	}
	
	return 0;
}
