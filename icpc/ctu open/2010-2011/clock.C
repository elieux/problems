#include <iostream>
#include <cstdlib>

using namespace std;

const char BASE[10] = { 119, 36,  93, 109, 46, 107, 123, 37, 127, 111};

int main()
{
	unsigned int S = 0;
	scanf("%d", &S );
	
	char digits[S][4];
	
	memset( digits, ' ', S * 80 * sizeof(char) );
	memset( posib, 0, S * 4 * sizeof(int) );
	
	for (register int i = 0 ; i < S; i++ )
	{
		for ( register int j = 0; j < 5; j++ )
		{
			char temp[20];
			fgets( temp, 20, stdin );
			if ( temp[1]
		}
	}*/
	
	return 0;
}